# Fingerprint Identity Blockchain System
# Environment Variables Configuration
# 
# Copy this file to .env and customize the values
# Usage: source .env && python3 main.py

# ==============================================
# DATABASE CONFIGURATION
# ==============================================

# Path to SQLite database file
# Default: fingerprint_blockchain.db (in current directory)
FP_DB_PATH=fingerprint_blockchain.db

# ==============================================
# BLOCKCHAIN CONFIGURATION
# ==============================================

# Mining difficulty (1-6)
# - 1: Very fast mining (less secure, good for testing)
# - 2: Fast mining (default, good balance)
# - 3-4: Medium mining (more secure)
# - 5-6: Slow mining (very secure, production)
FP_DIFFICULTY=2

# ==============================================
# SECURITY CONFIGURATION
# ==============================================

# Secret salt for fingerprint hash security (OPTIONAL)
# 
# When set, the system will hash the user's fingerprint hash
# with this salt before storing, adding an extra layer of security.
#
# IMPORTANT:
# - Leave empty for development/testing
# - Set a strong random value for production
# - NEVER change this value after storing data, or data becomes inaccessible!
# - Keep this value secret and backed up securely
#
# Example: Generate a random salt with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
#
FP_SECRET_SALT=

# ==============================================
# DOCKER CONFIGURATION (used in docker-compose.yml)
# ==============================================

# When running in Docker, the database path should point to
# the mounted volume for persistence:
# FP_DB_PATH=/app/data/fingerprint_blockchain.db
